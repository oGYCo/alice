=== TASK 6: CELERY + REDIS WORKER INFRASTRUCTURE ===
Date: 2026-02-26
Task: Create Celery worker package with task stubs, scheduling, and tests

FILES CREATED:
1. src/alice/worker/__init__.py
2. src/alice/worker/celery_app.py - Celery app factory with Redis broker
3. src/alice/worker/tasks.py - 6 stub pipeline tasks
4. src/alice/worker/scheduler.py - Beat schedule configuration  
5. tests/unit/test_worker.py - 14 comprehensive tests

CELERY CONFIGURATION:
- Broker: Redis (settings.CELERY_BROKER_URL)
- Result Backend: Redis (settings.REDIS_URL)
- Task Routes: 6 tasks routed to 3 queues (pipeline, fetch, push)
- Serialization: JSON (task_serializer, result_serializer, accept_content)
- Retry Config: max_retries=5, task_retry_backoff=True, backoff_max=1800s
- Time Limits: hard=600s, soft=540s
- Beat Schedule: fetch-all-sources every 1800s (30 minutes)
- Result Expiry: 3600s (1 hour)

STUB TASKS CREATED:
1. task_run_gatekeeper(content_id) - Stage 1: quality filter
2. task_run_understanding(content_id) - Stage 2: DeepSeek understanding
3. task_run_scoring(content_id) - Stage 3: quality scoring
4. task_run_indexing(content_id) - Stage 4: delivery indexing
5. task_fetch_all_sources() - Scheduled: fetch from all sources (30min)
6. task_push_batch(user_id) - Push content batch to user

ANTI-PATTERNS AVOIDED:
- NO Celery chains or groups (use individual tasks + DB state machine)
- NO real pipeline logic (stubs only, will be filled in Tasks 9-16)
- NO Flower monitoring
- NO pipeline state in Redis (PostgreSQL only)

TEST RESULTS:
✓ 14 tests pass (100%)
✓ All task registrations verified
✓ Configuration values validated
✓ Task output shapes verified
✓ Async execution (via .apply()) tested

RUFF LINTING:
✓ All checks passed
✓ Imports organized and sorted
✓ No unused imports

DESIGN DECISIONS:
1. Tasks are "stateless stubs" - they log and return stub responses
2. Real implementation happens in Tasks 9-16 when services are created
3. Each task has docstring explaining its pipeline stage and state transitions
4. Beat schedule directly in celery_app.py (avoids circular imports with scheduler.py)
5. Tasks use .bind=True for access to self (for retries, task ID, etc.)
6. Task names explicitly namespaced as "alice.worker.tasks.task_*"

PATTERNS ESTABLISHED:
- Celery app as module-level singleton (celery_app)
- Factory function for testability (create_celery_app)
- Individual tasks instead of chains/groups
- State machine responsibility: PostgreSQL (Task 13)
- Scheduler responsibility: Celery Beat + task_fetch_all_sources

NEXT TASKS:
- Task 7: LLM Client Abstraction & Mocking
- Task 8: Database Models (migration compatibility)
- Task 9: Gatekeeper Service (first real pipeline stage)
- Task 10: Understanding Service (DeepSeek integration)
- Task 11: Scoring Service
- Task 13: Pipeline Orchestrator (state machine)
