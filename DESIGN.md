# Alice — AI Secretary：个人智能信息管家

> **核心理念**：在信息过载的时代，Alice 不是又一个 RSS 阅读器或稍后阅读工具，而是一个**真正理解你的 AI 秘书**——她知道你懂什么、缺什么、正在做什么，并以此为基础为你从海量信息中精准筛选、排序、推送最值得你花时间的内容，同时持续维护和演进你的个人知识体系。

---

## 目录

1. [项目愿景与核心问题](#1-项目愿景与核心问题)
2. [系统总体架构](#2-系统总体架构)
3. [数据源接入层（Connector Layer）](#3-数据源接入层connector-layer)
4. [内容处理管道（Content Pipeline）](#4-内容处理管道content-pipeline)
5. [用户建模系统（User Modeling）](#5-用户建模系统user-modeling)
6. [智能推送引擎（Push Engine）](#6-智能推送引擎push-engine)
7. [内容分类体系](#7-内容分类体系)
8. [推送内容格式设计](#8-推送内容格式设计)
9. [知识表示与存储引擎](#9-知识表示与存储引擎)
10. [反馈与进化系统](#10-反馈与进化系统)
11. [破除信息茧房机制](#11-破除信息茧房机制)
12. [情境感知与动态调度](#12-情境感知与动态调度)
13. [交互层设计](#13-交互层设计)
14. [自动归档与报告生成](#14-自动归档与报告生成)
15. [认知衰退与间隔复习](#15-认知衰退与间隔复习)
16. [隐私、安全与成本](#16-隐私安全与成本)
17. [评估指标体系](#17-评估指标体系)
18. [技术选型参考](#18-技术选型参考)
19. [MVP 路线图](#19-mvp-路线图)

---

## 1. 项目愿景与核心问题

### 1.1 我们要解决的问题

当代知识工作者每天面对信息洪流：

| 信息源                         | 典型问题                         |
| ------------------------------ | -------------------------------- |
| arXiv / 学术论文               | 日均数百篇，无法逐一筛选         |
| X (Twitter)                    | 大佬高质量分享淹没在噪音中       |
| 微信公众号                     | 标题党严重，实质内容难以甄别     |
| 知乎 / Reddit / Stack Overflow | 高质量回答与灌水回答混杂         |
| 小红书                         | 偶有硬核内容但发现成本高         |
| YouTube / Bilibili             | 优质 UP 主内容与算法推荐脱节     |
| 播客                           | 时长长、信息密度参差不齐         |
| 技术博客 / Newsletter          | 分散在各处，难以统一管理         |
| 开源项目 Release Notes         | 关注的项目太多，重要更新容易遗漏 |

**核心矛盾**：信息的总量在指数增长，但人的注意力和时间是恒定的、稀缺的。

### 1.2 Alice 的核心价值主张

1. **精准过滤**：从海量信息中筛掉 90%+ 的噪音，只留下真正高质量的内容
2. **个性化排序**：不是"热门"排序，而是"对**当下的你**最有价值"排序
3. **知识图谱驱动**：系统维护你的个人知识图谱，推送不是盲目的，而是基于你"懂什么"和"缺什么"
4. **双向进化**：你在使用中训练系统，系统也在帮助你成长——形成正反馈飞轮
5. **输入到输出的闭环**：高质量输入最终导向高质量输出（报告、笔记、洞察）

---

## 2. 系统总体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         用户交互层 (Interaction Layer)                    │
│  ┌──────────────┐  ┌──────────────────┐  ┌────────────────────────┐     │
│  │ Telegram Bot │  │  Web Dashboard   │  │  API (扩展接口)         │     │
│  └──────┬───────┘  └────────┬─────────┘  └───────────┬────────────┘     │
└─────────┼──────────────────┼─────────────────────────┼──────────────────┘
          │                  │                         │
          ▼                  ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        推送引擎 (Push Engine)                            │
│  ┌───────────────┐  ┌────────────────┐  ┌──────────────────────┐        │
│  │ 优先级排序器    │  │ 情境调度器       │  │ 探索/利用平衡器       │        │
│  │ (Ranker)      │  │ (Scheduler)    │  │ (Exploration)        │        │
│  └───────────────┘  └────────────────┘  └──────────────────────┘        │
└──────────────────────────────┬──────────────────────────────────────────┘
                               │
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────────────────┐
│  用户建模系统      │ │ 知识图谱引擎     │ │  内容库 (Content Store)       │
│  (User Model)     │ │ (Knowledge       │ │  ┌──────────┐ ┌───────────┐ │
│  ┌─────────────┐  │ │  Graph Engine)   │ │  │ 结构化DB  │ │ 全文索引   │ │
│  │ 知识画像     │  │ │                  │ │  │(Postgres) │ │(Meilisearch│ │
│  │ 兴趣偏好     │  │ │  GraphRAG        │ │  │          │ │ / ES)      │ │
│  │ 阶段状态     │  │ │                  │ │  └──────────┘ └───────────┘ │
│  │ 反馈历史     │  │ │                  │ │                              │
│  └─────────────┘  │ └──────────────────┘ └──────────────────────────────┘
└──────────────────┘                                  ▲
                                                      │
┌─────────────────────────────────────────────────────┼──────────────────┐
│                    内容处理管道 (Content Pipeline)    │                  │
│  ┌───────────┐  ┌──────────┐  ┌──────────┐  ┌──────┴───────┐          │
│  │ 前置清洗    │→│ 内容理解  │→│ 质量评估  │→│ 入库 & 索引   │          │
│  │ (Cleaner)  │ │(Parser)  │  │(Scorer)  │  │(Indexer)     │          │
│  └───────────┘  └──────────┘  └──────────┘  └──────────────┘          │
└──────────────────────────────────┬─────────────────────────────────────┘
                                   │
┌──────────────────────────────────┴─────────────────────────────────────┐
│                    数据源接入层 (Connector Layer)                        │
│  ┌──────┐ ┌────┐ ┌──────┐ ┌──────┐ ┌────┐ ┌─────┐ ┌──────┐ ┌──────┐ │
│  │arXiv │ │ X  │ │微信   │ │知乎   │ │RSS │ │Reddit│ │GitHub│ │YouTube│ │
│  └──────┘ └────┘ └──────┘ └──────┘ └────┘ └─────┘ └──────┘ └──────┘ │
└───────────────────────────────────────────────────────────────────────┘
```

### 2.1 设计原则

| 原则                                    | 说明                                                       |
| --------------------------------------- | ---------------------------------------------------------- |
| **管道化（Pipeline）**                  | 每个阶段职责单一，可独立扩展和替换                         |
| **事件驱动（Event-Driven）**            | 新内容到达、用户反馈等均以事件形式驱动下游处理             |
| **渐进增强（Progressive Enhancement）** | 系统初始可以简单规则运行，随数据积累逐步引入更复杂的模型   |
| **离线优先（Offline-First）**           | 内容抓取、处理、评分均可异步离线完成，推送是最终的轻量动作 |
| **可观测性（Observability）**           | 每个环节都有日志、指标、追踪，便于调试和优化               |

---

## 3. 数据源接入层（Connector Layer）

### 3.1 接入策略

每个数据源作为一个独立的 **Connector** 插件，统一输出标准化的 `RawContent` 结构（仅供参考）：

```typescript
interface RawContent {
  source: SourceType;           // 来源平台
  sourceUrl: string;            // 原始链接
  sourceId: string;             // 平台内唯一 ID（用于去重）
  author: string;               // 作者
  title?: string;               // 标题（部分来源可能没有）
  rawText: string;              // 原始文本内容
  rawHtml?: string;             // 原始 HTML（可选）
  mediaUrls?: string[];         // 附带的图片/视频链接
  publishedAt: Date;            // 发布时间
  fetchedAt: Date;              // 抓取时间
  metadata: Record<string, any>; // 平台特有的元数据（点赞数、评论数等）
  language: string;             // 内容语言
}
```

### 3.2 各平台接入方案

| 数据源             | 接入方式                    | 更新频率    | 备注                                      |
| ------------------ | --------------------------- | ----------- | ----------------------------------------- |
| **arXiv**          | arXiv API + RSS             | 每日        | 按关键词/分类订阅                         |
| **X (Twitter)**    | X API v2 / Nitter RSS       | 实时~小时级 | 关注列表 + 关键词监控                     |
| **微信公众号**     | WeRSS / 搜狗微信 / 自建转发 | 小时级      | 该源获取难度较高，需要中转方案            |
| **知乎**           | RSS (zhihu-rss) / 爬虫      | 小时级      | 关注话题 + 关注用户                       |
| **小红书**         | 爬虫 + 关键词               | 小时级      | API 封闭，需反爬处理                      |
| **Reddit**         | Reddit API / RSS            | 小时级      | 按 subreddit 订阅                         |
| **Stack Overflow** | Stack Exchange API          | 每日        | 按标签订阅 + 高票筛选                     |
| **技术博客**       | RSS / Atom                  | 每日        | 支持用户自行添加 Feed                     |
| **播客**           | RSS (Podcast Feed)          | 每日        | 配合 Whisper 做音频转文字                 |
| **YouTube**        | YouTube Data API v3 / RSS   | 小时级      | 订阅频道 + Whisper 生成字幕               |
| **Bilibili**       | Bilibili API / RSS          | 小时级      | UP 主订阅 + 弹幕/评论辅助评估             |
| **GitHub**         | GitHub API / Release RSS    | 事件驱动    | Watch 的仓库的 Release、Issue、Discussion |
| **Newsletter**     | IMAP/邮件转发               | 实时        | 用户将邮件转发到指定地址                  |
| **Hacker News**    | HN API / Algolia            | 小时级      | Top Stories + 按关键词                    |

### 3.3 内容去重

多个数据源可能指向同一篇内容（如一篇博客同时出现在 X 分享、Reddit 讨论和 Hacker News 中）：

- **URL 归一化**：去除追踪参数（utm_*）、统一协议（http→https）、解析短链接
- **内容指纹**：对正文做 SimHash / MinHash，相似度超过阈值的合并为同一内容
- **聚合信号**：被多个独立社区提及的内容，可以作为质量信号的加分项

---

## 4. 内容处理管道（Content Pipeline）

信息从抓取到可推送，需要经过四级处理管道：

```
RawContent → [前置清洗] → [内容理解] → [质量评估] → [入库索引] → ProcessedContent
```

### 4.1 第一级：前置清洗（Gatekeeper）

**目标**：以极低成本快速过滤掉明显的垃圾内容，减轻下游负担。

**执行者**：轻量级本地模型（如微调后的 Qwen2.5-1.5B-Instruct）或低成本 API

**清洗规则**：

| 维度       | 拦截条件                       | 示例               |
| ---------- | ------------------------------ | ------------------ |
| 内容完整性 | 正文 < 100 字且无代码/公式     | 纯标题党、水贴     |
| 情绪密度   | 情绪词占比 > 60% 且无实证      | 纯情绪宣泄、引战帖 |
| 广告检测   | 包含推广链接/二维码/导流关键词  | 软文、广告         |
| 重复检测   | 与已入库内容指纹重复           | 洗稿、转载         |
| 时效检查   | 时效性内容已过期 > 5舔天        | 过期新闻           |
| 语言过滤   | 非用户配置的语言               | —                  |

**输出**：通过清洗的内容进入下一级 ✅，拦截的内容记录原因后归档 ❌（保留统计数据用于优化拦截规则）

### 4.2 第二级：内容理解（Content Understanding）

**目标**：深度理解内容，提取结构化信息。
**执行者**：高能力模型（如 DeepSeek API / Claude API / OpenAI API）
**提取信息**：

```typescript
interface ContentUnderstanding {
  // 内容摘要
  summary: string;                    // 内容核心概述
  keyPoints: string[];                // 核心要点列表
  
  // 结构化知识提取
  concepts: Concept[];                // 涉及的概念实体
  prerequisites: Concept[];           // 阅读所需前置知识
  contentSubgraph: ContentSubgraph;   // 该内容对应的概念关系子图
  
  // 分类标签
  category: ContentCategory;          // 硬核知识 / 思想性 / 时效性
  domains: string[];                  // 领域标签（ML, Systems, Math...）
  
  // 内容特征
  hasOriginalInsight: boolean;        // 是否有原创见解
  evidenceQuality: 'anecdotal' | 'empirical' | 'theoretical' | 'experimental';
  contentDensity: number;             // 信息密度评分 0-1
  estimatedReadTime: number;          // 预估阅读时间（分钟）
  
  // 多媒体处理
  transcription?: string;             // 音视频转文字
  visualDescriptions?: string[];      // 关键图表描述
}
```

```typescript
interface ContentSubgraph {
  contentId: string;
  nodes: Array<{ name: string; type: 'concept' | 'method' | 'tool' | 'theory' }>;
  edges: Array<{ from: string; to: string; relation: 'prerequisite' | 'extends' | 'applies_to' | 'contrasts' }>;
  difficulty: number;                 // 该内容的综合难度 0-1
  entryConcepts: string[];            // 最关键的前置入口概念
}
```

#### 多模态内容处理

| 内容类型   | 处理方式                                           |
| ---------- | -------------------------------------------------- |
| 纯文本     | 直接解析                                           |
| 图文混排   | OCR + 视觉模型理解图表                             |
| 视频       | Whisper 转文字 + 关键帧截取 → 视觉模型理解         |
| 播客/音频  | Whisper 转文字 → 文本管道处理                      |
| 代码仓库   | README + 核心代码结构分析                          |
| PDF (论文) | PDF 解析 → 分段理解（Abstract / Method / Results） |

### 4.3 第三级：质量评估（Quality Scoring）

**目标**：为每条内容打出多维度质量分。
**评分维度**（各维度 0-1，加权求和得到总分）：

$$Q_{total} = \sum_{i=1}^{n} w_i \cdot q_i$$

| 维度 $q_i$                   | 权重 $w_i$（默认） | 评估方法                           |
| ---------------------------- | ------------------ | ---------------------------------- |
| **实质性 (Substance)**       | 0.25               | 是否有原创观点、数据支撑、逻辑推演 |
| **信息密度 (Density)**       | 0.15               | 有效信息量 / 内容长度              |
| **可靠性 (Credibility)**     | 0.15               | 作者权威性、引用质量、同行评价     |
| **新颖性 (Novelty)**         | 0.20               | 相对于用户已有知识的增量信息       |
| **可操作性 (Actionability)** | 0.10               | 是否能直接指导行动或决策           |
| **社区信号 (Social Signal)** | 0.10               | 跨平台提及频率、高质量用户的互动   |
| **时效因子 (Timeliness)**    | 0.05               | 内容的时效性衰减                   |

**社区信号的计算**：不是简单的点赞数，而是**加权社区信号**——已知高质量用户（你关注的大佬）的点赞权重远高于普通用户。

### 4.4 第四级：入库与索引（Indexing）

通过质量评估的内容进入多维度存储：

1. **结构化存储**（PostgreSQL）：内容元数据、分类、评分、状态
2. **知识图谱**（Neo4j / Memgraph）：概念实体和关系，保存用户图谱与每条内容子图
   - **注意**：Neo4j GDS（Leiden 社区检测、PageRank 等算法）需要 Enterprise 版本。推荐在 Python 侧使用 NetworkX + leidenalg 库进行图算法计算。
3. **匹配结果索引**：记录 `content_subgraph ↔ user_kg` 的匹配分、未满足前置、推荐结论
4. **全文索引**（Meilisearch / Elasticsearch）：支持关键词搜索和高亮

---

## 5. 用户建模系统（User Modeling）

这是 Alice 的灵魂。系统必须构建对用户的深度理解，而非仅仅是一个标签列表。

### 5.1 用户画像的三层架构

```
┌─────────────────────────────────────┐
│         元认知层 (Meta-Cognitive)    │  ← 思维方式、学习风格、价值取向
│  "喜欢从第一性原理出发理解问题"          │
│  "偏好数学严谨性而非工程直觉"           │
├─────────────────────────────────────┤
│         能力图谱层 (Competency Map)   │  ← 知识图谱 + 掌握程度
│  "精通 Transformer 架构"              │
│  "了解但未深入 RLHF"                   │
│  "不熟悉 Diffusion Model 数学推导"     │
├─────────────────────────────────────┤
│         行为偏好层 (Behavioral)       │  ← 阅读习惯、时间偏好、互动模式
│  "工作日晚上偏好轻量内容"               │
│  "对论文类内容偏好精读、对资讯类偏好速览"  │
└─────────────────────────────────────┘
```

### 5.2 记忆分层系统

受 MemGPT / Letta 启发，将用户记忆分为三层：

| 记忆层                    | 容量 | 更新频率 | 内容                             | 衰减机制                   |
| ------------------------ | --- | ------- | -------------------------------- | -------------------------- |
| **工作记忆 (Working)**    | 小   | 实时     | 当前正在攻关的项目、本周阅读焦点 | 任务完成后归档             |
| **短期记忆 (Short-term)** | 中   | 每日     | 最近两周的阅读偏好漂移、临时兴趣 | 两周无再次触及则衰减       |
| **长期记忆 (Long-term)**  | 大   | 缓慢     | 已内化的核心知识、稳定的认知框架 | 极慢衰减，需要间隔复习触发 |

**工作记忆的特别设计**：
- 用户可以主动声明："我这周在研究 MoE 架构的负载均衡问题"
- 系统也可以从用户的阅读行为自动推断当前关注焦点
- 工作记忆直接影响推送排序权重——与当前工作直接相关的内容会被大幅提权

### 5.3 用户知识图谱与内容子图（Personal KG + Content Subgraph）

系统长期维护一个不断更新和迭代的用户知识图谱；同时每处理一条新内容，都生成该内容的子图。只有当内容子图与用户知识图谱匹配时，才认为该内容适合当前阶段、用户能够看懂并值得推送。

用户知识图谱不是静态标签集合，而是一个有**边权**有向图：

```
节点 (Node)：概念/知识点
  - name: "Attention Mechanism"
  - mastery: 0.85         // 掌握程度
  - lastReviewedAt: Date  // 上次复习时间
  - confidence: 0.9        // 系统对该评估的置信度

边 (Edge)：知识关系
  - type: "prerequisite" | "extends" | "contrasts" | "applies_to" | "inspired_by"
  - weight: float          // 关系强度
```

**用户知识图谱示例**：
```
[线性代数 ★★★★★] ──prerequisite──→ [Attention Mechanism ★★★★☆]
                                           │
                                      extends
                                           ▼
                                  [Flash Attention ★★★☆☆]
                                           │
                                      applies_to
                                           ▼
                                  [大模型推理优化 ★★☆☆☆]
```

当系统要推送一篇关于"Ring Attention"的论文时，它可以精确判断：
- ✅ 前置知识"Attention Mechanism"已掌握
- ⚠️ "Flash Attention"部分掌握，可能需要补充说明
- → 推送时附带 Flash Attention 的关键回顾

**每条内容的子图生成**：
- 从正文/图表/代码说明中提取核心概念与关系，形成 `ContentSubgraph`
- 自动标注该内容的关键前置概念 `entryConcepts`
- 计算内容难度 `difficulty`，作为阶段适配信号

**子图匹配判定（是否适合推送）**：

$$Match_{score} = 0.5 \cdot Prerequisite_{coverage} + 0.3 \cdot Concept_{distance\_fit} + 0.2 \cdot Difficulty_{fit}$$

| 匹配分段 | 推荐策略 | 含义 |
| -------- | -------- | ---- |
| `>= 0.75` | 直接进入推送候选池 | 当前阶段可读、可吸收 |
| `0.55 ~ 0.75` | 补充前置后推送 | 略超当前阶段，但可通过引导理解 |
| `< 0.55` | 暂缓推送，进入待成熟队列 | 当前阶段不适合，避免信息挫败 |

**关键约束**：`Match_score` 未达到阈值的内容，不参与当期 Top-K 推荐。

### 5.4 用户状态机

用户在不同时期有不同的模式，系统需要感知并适配：

```
  ┌──────────────────────────────────────────────────┐
  │                                                    │
  ▼                                                    │
[日常模式] ──用户声明──→ [项目攻关模式] ──项目完成──→ [日常模式]
  │                        │
  │                        ├── 推送权重：相关内容 ×3
  │                        ├── 非相关内容暂缓
  │                        └── 提升实时性要求
  │
  ├──→ [探索模式]（周末 / 用户主动触发）
  │      ├── 提高跨领域内容比例
  │      └── 降低实时性要求，侧重深度内容
  │
  └──→ [低能量模式]（深夜 / 用户标记疲惫）
         ├── 只推轻量时效类内容
         └── 暂停硬核推送
```

---

## 6. 智能推送引擎（Push Engine）

### 6.1 优先级排序器（Ranker）

推送排序不是单维度排序，而是基于多个信号的综合打分：

$$P_{score} = Q_{content} \cdot R_{relevance} \cdot T_{timing} \cdot D_{decay} \cdot U_{urgency} + \epsilon_{explore}$$

其中：
- $Q_{content}$：内容质量分（来自第三级评估）
- $R_{relevance}$：与用户知识图谱和当前状态的相关度
- $T_{timing}$：时间窗口匹配度（适合此刻推送的程度）
- $D_{decay}$：时效衰减因子（对时效性内容影响大，对知识类内容影响小）
- $U_{urgency}$：紧迫度（如 DDL 临近、重大更新）
- $\epsilon_{explore}$：探索噪声项（破除信息茧房）

### 6.2 相关度计算 $R_{relevance}$

```
R_relevance = α · KG_match       // 知识图谱匹配度
            + β · Working_match   // 与工作记忆的匹配度
            + γ · Preference_match // 与行为偏好的匹配度
            + δ · Gap_fill        // 填补知识空白的程度
```

其中：

```
KG_match = MatchScore(content_subgraph, user_kg)
MatchScore = λ1 · Prerequisite_coverage
           + λ2 · Concept_distance_fit
           + λ3 · Difficulty_fit
```

并设置推荐门槛：

```
if KG_match < θ_recommend:
    defer(content)  # 延后，不进入本轮推送
```

**关键洞察**：`Gap_fill` 是区别于传统推荐系统的核心——传统系统推你"喜欢的"，Alice 推你"需要但还不知道自己需要的"。

### 6.3 批量推送与节流

- **不是来一条推一条**，而是在推送窗口时从候选池中选出 Top-K 条内容
- 每次推送数量有上限（默认 3-5 条），避免信息轰炸
- 不同类别有配额：如每批至少 1 条硬核知识 + 最多 1 条时效信息
- 如果候选池中没有足够高质量的内容，**宁可不推也不注水**

### 6.4 推送时机决策（代码仅供参考）

```python
def should_push_now(user_context, content_batch):
    # 如果是紧急时效信息（如系统检测到用户关注的项目发布重大安全更新），立即推送
    if any(c.urgency == "critical" for c in content_batch):
        return True
    
    # 检查是否在用户配置的推送时间窗口内
    if not in_push_window(user_context.current_time, user_context.preferences):
        return False
    
    # 检查距离上次推送的间隔（避免过于频繁）
    if time_since_last_push(user_context) < user_context.min_push_interval:
        return False
    
    # 检查用户是否还有未处理的推送（堆积过多说明用户繁忙）
    if pending_push_count(user_context) > user_context.max_pending:
        return False
    
    return True
```

---

## 7. 内容分类体系

### 7.1 分类维度

```
内容类型
├── 硬核知识类 (Deep Knowledge)
│   ├── 学术论文 & 论文解读
│   ├── 技术深度博客
│   ├── 系统设计 & 架构分析
│   ├── 数学/科学基础
│   └── 技术问答（SO / Reddit 精选）
│
├── 思想性内容 (Thought-Provoking)
│   ├── 方法论 & 思维模型
│   ├── 行业洞见 & 趋势分析
│   ├── 跨学科连接
│   └── 哲学/认知科学视角
│
├── 时效信息类 (Time-Sensitive)
│   ├── 重大版本发布 & 更新
│   ├── 行业新闻 & 突发事件
│   ├── 会议 Deadline & 重要日期
│   └── 安全公告 & 紧急通知
│
├── 实践指导类 (Practical Guidance)
│   ├── 教程 & 手把手指南
│   ├── 最佳实践 & 经验分享
│   ├── 工具推荐 & 对比评测
│   └── 踩坑复盘 & 调试经验
│
└── 灵感素材类 (Inspiration)
    ├── 创意项目展示
    ├── 非常规应用案例
    └── 交叉领域创新
```

### 7.2 各类别的推送策略差异

| 类别     | 推送频率    | 信息时效 | 阅读深度期望 | 入库策略                |
| -------- | ----------- | -------- | ------------ | ----------------------- |
| 硬核知识 | 每日 1-3 条 | 低衰减   | 精读/选读    | 永久入库 + 知识图谱更新 |
| 思想性   | 每周 2-4 条 | 无衰减   | 精读 + 反思  | 永久入库 + 元认知层更新 |
| 时效信息 | 实时/每日   | 高衰减   | 速览         | 临时存储，定期清理      |
| 实践指导 | 按需        | 中衰减   | 选读/收藏    | 入库 + 标记适用场景     |
| 灵感素材 | 每周 1-2 条 | 无衰减   | 浏览         | 灵感库独立管理          |

---

## 8. 推送内容格式设计

### 8.1 Telegram 推送卡片（仅供参考，可以有更好的形式）

```
━━━━━━━━━━━━━━━━━━━━━━━━━
📚 硬核知识 · 预计阅读 12 分钟

FlashAttention-3: 利用硬件异步性加速
精确注意力计算

┈┈ 核心内容 ┈┈
本文提出了 FlashAttention-3，通过利用 GPU 的硬件
异步特性（如 WGMMA 指令重叠和 TMA 异步数据搬
运），在 H100 GPU 上将注意力计算速度提升至接近
理论峰值的 75%。关键创新在于将 softmax 计算与
矩阵乘法进行流水线重叠。

┈┈ 推送原因 ┈┈
🔗 你之前深入学习了 FlashAttention-2 的 tiling
策略，这篇论文是其直接后续。它在你已有的理解基
础上，仅新增了硬件异步重叠这一个核心概念。

┈┈ 前置补充 ┈┈
⚡ 你可能需要快速回顾一下 H100 的 TMA（Tensor 
Memory Accelerator）工作机制 → [简要说明附后]

┈┈ 阅读建议 ┈┈
建议精读 Section 3（核心算法），Section 2（背景）
可以快速扫过，你已经掌握了大部分内容。

🔗 原文链接

━━━━━━━━━━━━━━━━━━━━━━━━━
[👍 高质量]  [⏰ 稍后再看]  [📖 已知晓]
[👎 无价值]  [❓ 解释概念]  [💬 追问]
━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 8.2 不同类型的推送格式差异（仅供参考，可以有更好的形式）

**时效信息类（极简）**：
```
⚡ 时效信息

vLLM v0.8.0 发布

What: 支持了 Speculative Decoding 和新的 
PagedAttention v2 内存管理策略。
Impact: 你的推理服务可以直接升级获得约 30% 
的吞吐提升，但需要注意 API 有 breaking changes。

🔗 Release Notes   📋 变更日志
━━━━━━━━━━━━━━━
[✅ 已了解]  [📌 需要跟进]
```

**思想性内容**：
```
💡 思想性内容 · 预计阅读 8 分钟

"The Bitter Lesson" 的现代重读

Rich Sutton 的经典观察——"利用计算的通用方法最终
总会胜出"——在 LLM 时代得到了更强的印证。本文重
新审视了这一思想，并将其延伸到……

推送原因：这个思想框架可以帮助你重新审视当前在
做的模型优化方向选择——是投入更多工程调优，还是
押注更大规模的计算？

[👍 有启发]  [⏰ 稍后]  [👎 无感]  [💬 讨论]
```

### 8.3 应用前端卡片与详情页

前端中每一份经过系统解析后的内容，都必须以**可见卡片**形式展示（Telegram 端是推送简版，Web 端是完整工作台）。

**卡片层（列表页）**：

- 标题、来源、发布时间、类别
- AI 一句话摘要 + 推送原因
- 子图匹配分（`Match_score`）与推荐状态（适合 / 补充前置 / 暂缓）
- 快捷操作（高质量、稍后、已知晓、无价值、解释概念）

**卡片详情页（点击进入）**：

- 内容子图（该内容的概念节点、关系边、关键前置）
- AI 解析（核心观点、关键证据、阅读建议、为何适配你）
- 原始内容（原文/原链接/嵌入阅读器）
- 与用户整体知识图谱的映射路径（命中节点、缺失前置、可补全方向）
- 相关历史内容与交叉引用
- 用户笔记与追问区（对话记录可回流更新知识图谱）

---

## 9. 知识表示与存储引擎

### 9.1 GraphRAG 架构（不使用向量数据库）

Alice 不使用向量数据库，采用 **知识图谱 + 全文索引 + 规则过滤** 的 GraphRAG 架构。

```
查询："推荐一篇关于注意力机制优化的论文，适合我当前水平"

传统关键词检索：
  → 关键词匹配 "注意力机制优化" → 返回相关但未必适配当前阶段的内容

GraphRAG (Alice)：
  → 1. 在知识图谱中定位 "Attention Mechanism" 节点
  → 2. 查看用户对该节点的 mastery = 0.85
  → 3. 查找该节点的出边（extends, improves）
  → 4. 筛选 mastery < 0.5 的下游节点 → 找到 "Ring Attention"
  → 5. 在全文索引中检索 "Ring Attention" 相关的高质量内容
  → 6. 验证前置知识是否满足
  → 返回：精准匹配用户知识边缘的内容
```

### 9.2 存储架构

```
┌─────────────────────────────────────────────────┐
│                 统一查询层 (Query Layer)           │
│         GraphRAG Query Engine                     │
└──────────┬──────────┬──────────┬────────────────┘
	           │          │          │
    ┌──────▼──────┐ ┌─▼──────────┐ ┌▼───────────────┐
    │  Neo4j /    │ │ Meilisearch│ │  PostgreSQL     │
    │  Memgraph   │ │ / ES       │ │                 │
    │             │ │            │ │  内容元数据       │
    │  知识图谱    │ │ 全文索引    │ │  用户数据        │
    │  (概念、关系、│ │ (标题、正文、│ │  反馈记录        │
    │   用户掌握度) │ │ 标签、来源) │ │  推送历史        │
    │             │ │            │ │  系统配置        │
    └─────────────┘ └───────────┘ └─────────────────┘
```

### 9.3 知识图谱的自动维护

知识图谱不能完全依赖用户手动维护，需要自动化：

1. **内容处理时自动提取概念和关系**：每篇内容经过 LLM 理解后，自动提取其中的概念节点和关系边
2. **用户反馈驱动更新**：用户标记"已学会" → 更新 mastery；标记"太难" → 降低对应前置知识的 mastery 评估
3. **推断式更新**：如果用户轻松理解了 B 内容，而 B 依赖前置知识 A，则可以推断用户已具备 A → 自动提升 A 的 mastery
4. **定期一致性检查**：LLM 定期审视知识图谱，检测矛盾或不合理的评估

---

## 10. 反馈与进化系统

### 10.1 反馈类型与系统响应

| 用户反馈       | 系统理解             | 触发的更新动作                                                       |
| -------------- | -------------------- | -------------------------------------------------------------------- |
| 👍 **高质量**   | 内容匹配成功         | ① 强化当前推送策略 ② 更新知识图谱掌握度 ③ 提升该来源权重             |
| ⏰ **稍后再看** | 内容有价值但时机不对 | ① 保留在队列中，降低优先级 ② 在下一个合适的时间窗口重新推送          |
| 📖 **已知晓**   | 用户已掌握该知识     | ① 更新知识图谱（确认掌握） ② 减少同类重复推送 ③ 提升对用户水平的估计 |
| 👎 **无价值**   | 内容质量判断失误     | ① 降低该来源/该类型权重 ② 分析失误原因，优化评估模型 ③ 记录负面案例  |
| ❓ **解释概念** | 用户有知识缺口       | ① 触发 QA 对话 ② 记录知识缺口到图谱 ③ 后续补充相关前置知识推送       |
| 💬 **追问**     | 用户想深入交流       | ① 进入自由对话模式 ② 对话内容纳入用户画像更新                        |

### 10.2 Skill 系统设计

系统将更新逻辑抽象为可配置的 **Skills**，不同反馈触发不同的 Skill 组合：

```yaml
skills:
  # 知识图谱更新 Skill
  update_knowledge_graph:
    trigger: ["positive_feedback", "learned_new"]
    actions:
      - extract_concepts_from_content
      - update_mastery_scores
      - add_new_edges_if_discovered
    
  # 偏好调整 Skill  
  adjust_preferences:
    trigger: ["negative_feedback", "already_known"]
    actions:
      - analyze_mismatch_reason
      - update_source_weights
      - update_topic_weights
      - log_to_feedback_history
    
  # 难度校准 Skill
  calibrate_difficulty:
    trigger: ["too_hard", "too_easy", "explain_concept"]
    actions:
      - reassess_user_mastery
      - recalculate_prerequisite_gaps
      - adjust_content_difficulty_range
    
  # 兴趣发现 Skill
  discover_interest:
    trigger: ["explore_new_topic", "positive_on_unexpected"]
    actions:
      - add_new_interest_node
      - expand_connector_subscriptions
      - schedule_introductory_content

  # 周期性自省 Skill（非反馈触发）
  periodic_self_review:
    trigger: ["weekly_cron"]
    actions:
      - review_feedback_statistics
      - detect_preference_drift
      - identify_knowledge_gaps
      - generate_user_model_diff_report
```

### 10.3 冷启动策略

新用户没有任何历史数据时的处理：

1. **引导式对话**：通过 Telegram 的交互式对话，了解用户的基本信息
   - "你目前的主要工作/研究方向是什么？"
   - "你在以下领域的熟悉程度如何？" → 提供滑块
   - "你最近在关注哪些话题？"
   - "你每天大约有多少时间用于阅读学习？"
2. **导入已有数据**：
   - 允许导入浏览器书签/收藏夹
   - 连接 GitHub（分析 Star 仓库和贡献领域）
   - 导入 Notion / Obsidian 知识库（快速构建初始知识图谱）
   - 导入 Pocket / Instapaper 稍后阅读列表
3. **渐进式校准**：前两周为"校准期"，增大推送的多样性和探索比例，快速收集反馈数据

---

## 11. 破除信息茧房机制

### 11.1 $\epsilon$-Greedy 探索策略

在推送排序器中保留探索空间：

$$
P_{push} = \begin{cases} 
\text{Top-K from ranker} & \text{with probability } 1 - \epsilon \\
\text{Random from high-quality pool} & \text{with probability } \epsilon
\end{cases}
$$

其中 $\epsilon$ 默认为 0.08（即约 8% 的推送为探索性内容），可由用户调节。

### 11.2 高级探索策略

除了简单的 $\epsilon$-greedy，还引入以下机制：

1. **跨域桥接推送**：当系统检测到两个看似无关的领域之间存在深层联系时，主动推送桥接内容
   - 示例：用户精通编译器优化，而一篇关于神经网络编译（如 TVM/MLIR）的内容恰好架起了桥梁

2. **认知边界扩展**：定期推送距离用户知识图谱"恰好一步"之外的内容
   - 不是随机探索，而是图谱上的**邻域探索**——你可能不知道自己对这个方向感兴趣

3. **Serendipity 引擎**：分析人类创新史中的跨领域启发案例，构建"意外发现"推荐模型
   - 灵感来源：牛顿的棱镜实验源于对"好奇心"的探索，而非对光学的系统研究

4. **用户可调探索度**：
   - 🐌 保守模式：$\epsilon = 0.03$，几乎全部是相关内容
   - 🚶 平衡模式：$\epsilon = 0.08$，偶尔的惊喜
   - 🚀 探索模式：$\epsilon = 0.20$，大量新领域曝光

### 11.3 探索质量保证

- 探索性推送必须通过质量评估下限（$Q_{total} > 0.7$），确保即使是跨界内容也是高质量的
- 对探索性推送的用户反馈单独追踪，用于优化探索策略本身
- 探索推送会特别标注："🌍 这是一篇跨领域推荐——它可能与你熟悉的 X 领域有意想不到的联系"

---

## 12. 情境感知与动态调度

### 12.1 时间窗口策略

```yaml
push_schedule:
  weekday:
    morning:        # 7:00-9:00
      type: [time_sensitive, practical_guidance]
      max_count: 2
      depth: light
      note: "通勤时间，适合速览"
    
    work_hours:     # 9:00-12:00, 14:00-18:00
      type: [deep_knowledge]
      max_count: 2
      depth: deep
      note: "黄金工作时间，推送与当前项目最相关的硬核内容"
    
    lunch:          # 12:00-14:00
      type: [thought_provoking, inspiration]
      max_count: 1
      depth: medium
      note: "午间适合轻松但有深度的思考性内容"
    
    evening:        # 18:00-22:00
      type: [thought_provoking, deep_knowledge]
      max_count: 3
      depth: medium_to_deep
      note: "晚间学习时间"
    
    late_night:     # 22:00-7:00
      type: [time_sensitive]    # 仅紧急信息
      max_count: 1
      depth: light
      note: "除非紧急否则不打扰"

  weekend:
    type: [deep_knowledge, thought_provoking, inspiration]
    max_count: 5
    depth: deep
    note: "周末适合深度阅读和探索"
```

### 12.2 动态调整信号

| 信号                   | 检测方式          | 调度影响                       |
| ---------------------- | ----------------- | ------------------------------ |
| 用户很久没处理推送     | 推送积压数 > 阈值 | 降低推送频率，发送"是否暂停？" |
| 用户快速处理多条推送   | 反馈速度加快      | 适当增加推送量                 |
| 用户标记"项目攻关模式" | 主动声明          | 进入战时状态（见 12.3）        |
| 节假日                 | 日历 API          | 调整为休闲模式                 |
| 用户标记"旅行中"       | 主动声明          | 暂停除紧急信息外的所有推送     |

### 12.3 战时状态（Project Mode）

用户声明："我这两周在集中做 CUDA Kernel 优化"

系统响应：
- 📡 数据源侧：临时增加 CUDA / GPU 相关的抓取关键词和 RSS 源
- ⚖️ 排序器侧：CUDA 相关内容的 $R_{relevance}$ 系数 × 3
- 📵 调度器侧：非 CUDA 相关的非紧急内容暂缓推送
- 📊 退出时：生成项目期间的知识积累总结报告

---

## 13. 交互层设计

### 13.1 Telegram Bot (aiogram v3)

**核心功能**：

| 命令                  | 功能                       |
| --------------------- | -------------------------- |
| `/start`              | 引导冷启动流程             |
| `/feed`               | 手动触发一次推送           |
| `/focus <topic>`      | 进入项目攻关模式           |
| `/unfocus`            | 退出项目攻关模式           |
| `/explore`            | 手动触发一批探索性推送     |
| `/pause [duration]`   | 暂停推送                   |
| `/stats`              | 查看本周阅读统计           |
| `/graph`              | 查看知识图谱概览（简化版） |
| `/add <url>`          | 手动添加一条内容让系统处理 |
| `/subscribe <source>` | 添加新的订阅源             |
| `/settings`           | 进入设置面板               |

**追问机制**：

在任何推送卡片下点击"❓ 解释概念"后：
1. 弹出概念选择器（系统自动从推送内容中提取可能需要解释的概念）
2. 用户选择后，系统基于用户知识图谱生成个性化的解释
3. 解释会从用户"已知"的概念出发进行类比和推导
4. 对话结束后，系统更新知识图谱

### 13.2 Web Dashboard

**信息架构**：

```
Dashboard
├── 📥 内容卡片流 (Feed Cards)
│   ├── 全量解析内容卡片（所有已解析内容可见）
│   ├── 待阅读队列（按优先级排序）
│   ├── 今日推送
│   └── 历史推送（可搜索、可过滤）
│
├── 🧠 知识图谱 (Knowledge Graph)
│   ├── 交互式图谱可视化（可缩放、拖拽）
│   ├── 知识掌握热力图
│   ├── 知识缺口分析
│   ├── 学习路径建议
│   └── 全局图谱管理（节点编辑、关系维护、掌握度校准、合并去重）
│
├── 📊 数据洞察 (Insights)
│   ├── 阅读统计（时间、数量、领域分布）
│   ├── 知识增长曲线
│   ├── 信息来源质量排名
│   └── 推送精准度趋势
│
├── 📚 知识库 (Library)
│   ├── 收藏夹
│   ├── 笔记本（用户对内容的批注）
│   ├── 周/月度总结报告
│   └── 导出功能（Markdown / PDF / Notion）
│
├── ⚙️ 设置 (Settings)
│   ├── 数据源管理
│   ├── 推送偏好
│   ├── 时间窗口配置
│   ├── 探索度调节
│   └── 个人资料 & 知识自评
│
├── 🛠 前端管理台 (Admin)
│   ├── Connector 管理（启停、抓取频率、失败重试）
│   ├── 管道监控（清洗/理解/评分/入库实时状态）
│   ├── 子图匹配审计（为什么推、为什么不推）
│   ├── 策略调参（匹配阈值、类别配额、探索比例）
│   └── 成本与模型路由（Token、延迟、失败率）
│
└── 🔍 搜索 (Search)
    ├── 全文搜索（已收录内容）
    ├── 自然语言检索（Query 重写 + 全文 + 图谱）
    └── 图谱搜索（按概念探索）
```

**核心交互约束**：

- 每条被系统解析通过的内容都出现在卡片流中，不做“黑盒隐藏”
- 点击任一卡片必须可见三块核心信息：`内容子图`、`AI 解析`、`原始内容`
- 知识图谱页面提供全局管理能力，而不仅是只读可视化

### 13.3 前端管理设计原则

- **一个前端管理全系统**：用户工作台与管理台统一在同一套前端内，通过权限区分视图
- **可解释性优先**：每条推荐展示 `content_subgraph ↔ user_kg` 的匹配路径与差异
- **可观测性优先**：关键链路（抓取→清洗→理解→评分→推送）提供实时状态与告警
- **可操作闭环**：支持一键重跑内容处理、一键调整阈值、一键回溯推荐决策
- **多端可用**：桌面端深度管理，移动端聚焦阅读与反馈

### 13.4 API 设计（扩展性）

提供 RESTful + WebSocket API，支持第三方集成：

- 其他 IM 接入（Discord、Slack、微信）
- 浏览器插件（阅读网页时可一键送入 Alice 处理）
- Obsidian / Notion 插件（双向同步知识库）
- CLI 工具（`alice add <url>`, `alice feed`）

---

## 14. 自动归档与报告生成

### 14.1 周报/月报自动生成

当系统检测到用户在某个主题上积累了足够的阅读量时，自动生成结构化报告：

```markdown
# 📋 Alice 周报 — 2026.02.19 ~ 2026.02.25

## 本周学习概览
- 阅读了 12 篇内容（6 篇硬核知识 + 3 篇思想性 + 3 篇时效信息）
- 知识图谱新增 4 个概念节点，更新了 8 个掌握度评估
- 主要聚焦领域：分布式推理、MoE 架构

## 核心收获
### 1. MoE 负载均衡的最新进展
[综合3篇论文和2篇博客的要点，相互联系进行阐述]

### 2. vLLM 0.8.0 的架构变化对你的影响
[结合你当前项目的上下文进行分析]

## 知识图谱变化
- ✅ 新掌握：Expert Parallelism, GShard Routing
- 📈 提升：MoE Architecture (0.4 → 0.7)
- 🔍 发现的知识缺口：Token Dropping 策略

## 下周推荐关注
- 基于本周的学习轨迹，建议下周关注 [具体建议]
```

### 14.2 主题聚合报告

当用户在某个特定主题上点赞了 5+ 篇相关内容后，系统自动提议生成一份该主题的深度综合报告：

- 整合所有相关内容的核心观点
- 构建知识脉络（从基础到前沿）
- 标注共识、争议和开放问题
- 输出为 Markdown，可一键转 PDF
- 可作为用户写文章/做分享的素材底稿

### 14.3 学习轨迹可视化

系统追踪用户的长期知识成长：

- 知识图谱的演化动画（每月对比）
- 阅读领域的桑基图（时间 → 领域 → 深度）
- 个人知识与领域前沿的差距分析

---

## 15. 认知衰退与间隔复习

### 15.1 基于 FSRS（Free Spaced Repetition Scheduler）的复习调度

对用户标记为"高价值"的硬核知识，按以下间隔复习：

$$R(t) = e^{-t/S}$$

其中 $R(t)$ 为记忆保持率，$t$ 为时间。FSRS 是一种现代化的间隔复习算法，相比传统艾宾浩斯曲线更加精确和自适应。

**默认复习间隔**：1天 → 3天 → 7天 → 14天 → 30天 → 90天

### 15.2 复习推送的设计

复习推送不是简单的原文重推，而是：

1. **概念测试**："你还记得 FlashAttention 的核心优化策略是什么吗？"（先提问，后展示答案）
2. **关联新知**："你之前学过 X，最近有一篇新的论文从不同角度阐述了类似想法"
3. **应用提示**："你当前项目中的 Y 问题，是否可以借鉴之前学过的 Z 方法？"

### 15.3 自适应调整

- 如果用户在复习中快速回忆起来 → 延长下次间隔（$S$ 增大）
- 如果用户表示已经遗忘 → 缩短间隔并补充相关基础内容
- 如果某个知识点被频繁应用于实际工作中 → 标记为"已内化"，退出复习队列

---

## 16. 隐私、安全与成本

### 16.1 隐私设计原则

- **数据主权**：用户的所有数据（知识图谱、反馈历史、个人画像）完全归用户所有
- **本地优先**：支持完全自托管部署，敏感数据不出本地
- **最小权限**：各 Connector 只请求必要的最小权限
- **可导出/可删除**：用户可以随时导出所有数据或完全删除

### 16.2 安全考虑

- 数据库加密存储
- API Token 安全管理（使用 Vault 或加密 env）
- Telegram Bot 通信加密
- 内容抓取的 Rate Limiting（避免触发平台封禁）

### 16.3 成本优化策略

LLM API 调用是最大的成本来源，需要精细优化：

| 策略               | 说明                                                 | 预估节省            |
| ------------------ | ---------------------------------------------------- | ------------------- |
| **分层模型调用**   | 前置清洗用轻量本地模型（Qwen 1.5B/3B），深度理解才用大模型 API | 60-70%              |
| **内容缓存**       | 相似内容的评估结果可复用                             | 15-20%              |
| **批量处理**       | 累积一批内容后批量调用，减少 API 开销                | 10-15%              |
| **摘要缓存**       | 同一内容跨用户共享摘要（多用户场景）                 | 20-30%              |
| **智能跳过**       | 对明显不相关的内容跳过深度理解环节                   | 30-40%              |

**月度成本估算**（单用户，每日处理约 200 条原始内容）：

| 组件                         | 月成本（USD，估算） |
| ---------------------------- | ------------------- |
| LLM API（大模型，深度理解）  | $15-30              |
| LLM API（小模型/本地，清洗） | Ollama 推理资源，无 API 成本 |
| 关系数据库（云托管）         | $5-10               |
| 服务器/VPS                   | $10-20              |
| **总计**                     | **$30-65/月**       |

---

## 17. 评估指标体系

### 17.1 推送质量指标

| 指标               | 计算方式                            | 目标值         |
| ------------------ | ----------------------------------- | -------------- |
| **正反馈率**       | 👍 / (👍+👎+📖)                         | > 70%          |
| **无价值率**       | 👎 / 总推送                          | < 5%           |
| **已知晓率**       | 📖 / 总推送                          | < 15%          |
| **稍后再看消化率** | 最终阅读的"稍后再看" / 总"稍后再看" | > 60%          |
| **追问率**         | ❓ / 总推送                          | 10-20%（适中） |
| **推送打开率**     | 打开 / 推送                         | > 80%          |

### 17.2 系统健康指标

| 指标               | 说明                           | 目标值                  |
| ------------------ | ------------------------------ | ----------------------- |
| **内容管道吞吐**   | 每小时处理的原始内容数         | > 100                   |
| **抓取成功率**     | 成功抓取 / 尝试抓取            | > 95%                   |
| **端到端延迟**     | 内容发布 → 推送到用户          | 时效类 < 1h，其他 < 24h |
| **知识图谱覆盖率** | 有掌握度评估的节点 / 总节点    | > 60%                   |
| **用户模型置信度** | 系统对用户画像评估的平均置信度 | > 0.7                   |

### 17.3 长期价值指标

- **知识图谱增长率**：用户知识图谱每月新增节点数
- **知识深化率**：已有节点 mastery 提升的比例
- **输出转化率**：基于 Alice 推送的内容产出的笔记/文章/报告数量
- **用户留存**：连续使用天数 / 周活跃度

---

## 18. 技术选型参考

### 18.1 后端架构

| 组件           | 推荐选型                  | 备选                     |
| -------------- | ------------------------- | ------------------------ |
| **主语言**     | Python (FastAPI)          | Go, TypeScript (Bun)     |
| **任务队列**   | Celery + Redis / Dramatiq (个体任务执行，NO Celery chains - 一个失败会导致整条链中断，而是使用 DB 状态机) | Temporal, BullMQ         |
| **消息总线**   | Redis Streams / NATS      | Kafka（大规模时）        |
| **关系数据库** | PostgreSQL                | —                        |
| **图数据库**   | Neo4j                     | Memgraph, FalkorDB       |
| **全文搜索**   | Meilisearch               | Elasticsearch            |
| **缓存**       | Redis (appendonly yes 配置持久化)                  | —                        |
| **对象存储**   | MinIO (自托管) / S3       | —                        |

### 18.2 AI/ML 组件

| 组件             | 推荐选型                                      |
| ---------------- | --------------------------------------------- |
| **大模型 API**    | DeepSeek API (推荐) / Claude API / OpenAI API |
| **本地轻量模型**   | Qwen 1.5B/3B via Ollama (RTX 4060 Laptop, ~1.2GB VRAM) |
| **音频转文字**     | Whisper (本地) / Groq Whisper API             |
| **PDF 解析**      | marker / nougat / pymupdf4llm                 |
| **知识图谱构建**   | LLM + 自定义 prompt chain                     |

### 18.3 前端

| 组件           | 推荐选型                                 |
| -------------- | ---------------------------------------- |
| **Web 框架**   | Next.js (React)                          |
| **图谱可视化** | D3.js / Cytoscape.js / react-force-graph |
| **UI 组件库**  | shadcn/ui                                |
| **状态管理**   | Zustand / TanStack Query                 |
| **实时通信**   | WebSocket / SSE                          |
| **权限控制**   | NextAuth + RBAC                          |
| **监控可视化** | ECharts / Recharts                       |

### 18.4 基础设施

| 组件             | 推荐选型                  |
| ---------------- | ------------------------- |
| **容器化**       | Docker + Docker Compose   |
| **编排（可选）** | K3s（轻量 K8s）           |
| **CI/CD**        | GitHub Actions            |
| **监控**         | Prometheus + Grafana      |
| **日志**         | Loki / structured logging |

---

## 19. MVP 路线图

### Phase 0：基础验证（2-3 周）
- [ ] 搭建项目骨架（FastAPI + PostgreSQL + Redis）
- [ ] 实现 2-3 个核心 Connector（RSS/Atom、arXiv 为主）
- [ ] 实现基础内容处理管道（清洗 + LLM 摘要）
- [ ] 实现最简 Telegram Bot（推送 + 基础反馈按钮）
- [ ] 验证端到端流程可跑通

### Phase 1：核心能力（4-6 周）
- [ ] 实现完整的内容质量评估体系
- [ ] 实现基础用户建模（兴趣标签 + 偏好权重）
- [ ] 实现优先级排序器（基于规则 + 简单打分）
- [ ] 扩展更多 Connector（X、Reddit、知乎等）
- [ ] 实现推送时间窗口调度
- [ ] 实现多种反馈类型及对应的系统响应

### Phase 2：知识引擎（4-6 周）
- [ ] 引入全文索引与图谱联合检索（不使用向量数据库）
- [ ] 构建用户知识图谱（GraphRAG 基础版）
- [ ] 为每条内容生成内容子图（概念、关系、前置）
- [ ] 实现内容子图与用户图谱匹配打分（含推荐阈值门禁）
- [ ] 实现知识图谱驱动的推送排序
- [ ] 实现前置知识补充推送
- [ ] 实现"追问"QA 对话功能
- [ ] 开始开发 Web Dashboard（基础版：卡片流 + 图谱管理 + Admin）

### Phase 3：智能进化（4-6 周）
- [ ] 实现三层记忆系统
- [ ] 实现信息茧房破除机制
- [ ] 实现项目攻关模式（战时状态）
- [ ] 实现认知衰退复习调度
- [ ] 实现自动周报/月报生成
- [ ] Web Dashboard 完善（知识图谱可视化、数据洞察、前端管理台）

### Phase 4：打磨与扩展（持续）
- [ ] 冷启动体验优化
- [ ] 推送精准度持续调优
- [ ] 更多平台 Connector
- [ ] 浏览器插件 / Obsidian 插件
- [ ] 多用户支持
- [ ] 性能优化和成本优化

---

> **Alice 不只是一个工具，而是你的数字化认知伙伴。她的目标不是让你读更多，而是让你的每一分钟阅读时间都产生最大价值。**
