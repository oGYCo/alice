# Alice 后续开发完整文档

**生成时间：** 2026-02-27
**分析范围：** 整个代码仓库（backend, frontend, tests, configs）
**文档目的：** 指导后续开发，补全功能集成，修复已知问题

---

## 📋 执行摘要

经过深入分析，Alice 项目在代码**广度**上非常出色（20+ 核心服务全部实现），但在**集成深度**上存在关键缺口。**Phase 2/3 的高级功能（匹配、排序、记忆系统、FSRS、GraphRAG）已经完整实现并通过单元测试，但尚未连接到主流水线或用户可见的业务流程中。**

### 核心发现

- ✅ **已实现服务数量：** 20+
- ⚠️ **主流水线集成率：** ~60%
- ❌ **关键服务未接入：** 7 个（Matching, Ranking, FSRS, Memory, PushScheduler, GraphRAG, KGUpdater feedback loop）
- 📊 **测试覆盖：** 43 个单元测试文件，7 个集成测试文件，**0 个前端 E2E 测试文件**
- 🕒 **定时任务：** 仅 1 个生效（应有 5-7 个）

### 优先级建议

**当前阶段应聚焦于"集成工作"而非新功能开发。** 代码已经为 Phase 2 目标做好准备，只需要完成服务间的连接。

---

## 1. 🔴 紧急问题（P0 - 生产就绪前必须解决）

### 1.1 MatchingService 未集成到推送批次选择

**问题描述：**
- `src/alice/services/matching.py` 已完整实现基于知识图谱的相关性评分
- 单元测试已通过 (`tests/unit/test_matching.py`)
- 集成测试已通过 (`tests/integration/test_phase2_integration.py`)
- **但该服务从未在生产代码中被调用**

**影响范围：**
- 推送内容无法基于用户知识图谱进行个性化筛选
- `RankingService.compute_p_score()` 的 `r_relevance` 参数固定为 1.0（占位符值）
- 用户收到的内容未经匹配度计算

**修复方案：**

1. 在 `src/alice/services/push.py` 的 `get_ready_for_push()` 方法中集成 MatchingService
2. 为每个候选内容计算 `r_relevance` 分数
3. 将结果传递给 `RankingService.compute_p_score()`

**涉及文件：**
- `src/alice/services/push.py`（修改）
- `src/alice/services/matching.py`（已存在，无需修改）
- `src/alice/services/ranking.py`（已存在，可能需要微调参数传递）

**验收标准：**
- 推送批次按实际计算的相关性分数排序
- 集成测试覆盖 push → matching → ranking 完整链路

---

### 1.2 RankingService P_score 未应用于推送排序

**问题描述：**
- `src/alice/services/ranking.py` 已实现完整的 P_score 公式（Q_quality × R_relevance × T_timing + epsilon_explore）
- 数据库已有 `p_score` 字段（Alembic migration）
- **但推送批次查询未按 `p_score` 排序**

**影响范围：**
- 用户看到的内容顺序仍是随机或按时间排序
- 高质量高相关性内容未优先推送
- 探索/利用平衡策略未生效

**修复方案：**

1. 在 `task_run_scoring` 之后增加一步：调用 `RankingService.compute_p_score()` 计算并更新 `p_score` 字段
2. 修改 `PushService.get_ready_for_push()` 的查询，添加 `ORDER BY p_score DESC`
3. 实现定时任务：每日批量更新所有未推送内容的 `p_score`（考虑时间衰减）

**涉及文件：**
- `src/alice/services/push.py`（修改查询排序逻辑）
- `src/alice/pipeline/tasks.py`（在 scoring 后增加 ranking 步骤）
- `src/alice/worker/scheduler.py`（新增定时任务）

**验收标准：**
- 推送批次按 `p_score` 降序排列
- 新内容入库后自动计算 `p_score`
- 定时任务每日更新旧内容的 `p_score`

---

### 1.3 task_retry_failed 已实现但未加入 Celery Beat 调度

**问题描述：**
- `src/alice/pipeline/tasks.py` 中已定义 `task_retry_failed` 任务
- **但 `src/alice/worker/scheduler.py` 的 `BEAT_SCHEDULE` 中未包含该任务**
- 失败的内容无法自动重试，需手动触发

**影响范围：**
- 临时网络错误导致的失败内容永久停滞
- 运维负担增加

**修复方案：**

在 `src/alice/worker/scheduler.py` 中添加：

```python
"retry-failed-every-6-hours": {
    "task": "alice.pipeline.tasks.task_retry_failed",
    "schedule": 21600.0,  # 6 hours
    "options": {"queue": "pipeline"},
},
```

**涉及文件：**
- `src/alice/worker/scheduler.py`（修改）

**验收标准：**
- Celery Beat 日志显示每 6 小时执行一次 `task_retry_failed`
- 失败内容在下一次调度时自动重试

---

### 1.4 GraphRAG 查询引擎已实现但未暴露 API 端点

**问题描述：**
- `src/alice/services/graphrag_query.py` 实现了完整的混合搜索（图谱 + 全文 + 语义占位符）
- **但 `/api/v1/search` 端点仅使用 Meilisearch，完全忽略 GraphRAG**
- 前端搜索功能无法利用知识图谱

**影响范围：**
- 用户无法基于概念关系搜索内容
- 已实现的图谱功能被浪费
- 搜索结果质量低于预期

**修复方案：**

1. 新增 API 端点：`POST /api/v1/search/hybrid`，调用 `GraphRAGQueryEngine`
2. 或改造现有 `/api/v1/search` 端点，添加参数 `mode=simple|hybrid`
3. 更新前端搜索页面，支持切换搜索模式

**涉及文件：**
- `src/alice/api/v1/search.py`（新增或修改端点）
- `src/alice/services/graphrag_query.py`（已存在，无需修改）
- `frontend/src/app/search/page.tsx`（前端集成）

**验收标准：**
- API 端点返回基于图谱的混合搜索结果
- 前端页面可切换搜索模式
- 集成测试覆盖 hybrid search 完整流程

---

### 1.5 反馈 API 未触发 KG 更新

**问题描述：**
- 用户通过 `/api/v1/feedback` 提交反馈后，数据仅存储在数据库中
- **`src/alice/services/kg_updater.py` 的 `update_on_feedback()` 方法从未被调用**
- 用户的 KNOWS 关系和 mastery 分数未随反馈更新

**影响范围：**
- 知识图谱无法反映用户真实学习进度
- 推荐系统缺乏用户状态信号
- Phase 2 的"反馈闭环"未完成

**修复方案：**

在 `src/alice/api/v1/feedback.py` 的反馈接口中：

1. 创建 `KGUpdaterService` 实例
2. 调用 `await kg_updater.update_on_feedback(user_id, content_id, rating, comment)`
3. 可选：使用 Celery 异步任务处理（避免阻塞 API 响应）

**涉及文件：**
- `src/alice/api/v1/feedback.py`（修改）
- `src/alice/services/kg_updater.py`（已存在，无需修改）
- 可选：`src/alice/pipeline/tasks.py`（新增异步任务）

**验收标准：**
- 提交反馈后，Neo4j 中相应的 KNOWS 关系 mastery 值更新
- 集成测试覆盖反馈 → KG 更新流程

---

### 1.6 环境变量文档缺失 ALICE_API_KEY

**问题描述：**
- `.env.example` 中未包含 `ALICE_API_KEY`
- 该变量是后端 API 鉴权的核心，缺失会导致 401 错误
- 新用户无法正确配置环境

**影响范围：**
- 新开发者首次运行时遇到困惑
- 生产部署可能遗漏关键配置

**修复方案：**

在 `.env.example` 中添加：

```dotenv
# Auth
ALICE_API_KEY=alicesecret
```

并在 README.md 中明确说明该变量的用途。

**涉及文件：**
- `.env.example`（修改）
- `README.md`（补充说明）

**验收标准：**
- `.env.example` 包含所有必需的环境变量
- README 清晰说明 API key 的作用和配置方法

---

## 2. 🟠 功能完整性问题（P1 - Phase 2/3 目标）

### 2.1 PushScheduler 未集成到推送触发逻辑

**问题描述：**
- `src/alice/services/push_scheduler.py` 已实现静默时段、推送窗口、内容类型匹配
- **但手动推送触发（API 或 Celery 任务）完全忽略调度逻辑**
- 用户设置的推送偏好未生效

**修复方案：**

1. 创建新的 Celery Beat 任务：`task_schedule_push_batches`
2. 遍历所有用户，调用 `PushScheduler.should_push_now(user)` 判断是否应推送
3. 使用 `PushScheduler.get_content_type_for_window()` 选择适合当前时间窗口的内容类型
4. 调用 `task_push_batch.delay(user_id, content_type_filter)`

**涉及文件：**
- `src/alice/worker/scheduler.py`（新增定时任务）
- `src/alice/services/push_scheduler.py`（已存在，可能需微调接口）
- `src/alice/pipeline/tasks.py` 或 `src/alice/worker/tasks.py`（新增任务）

**验收标准：**
- 静默时段内不推送内容
- 推送频率符合用户设置
- 不同时间窗口推送不同类型内容（早上看新闻，晚上看深度文章）

---

### 2.2 MemoryManager 未集成到排序逻辑

**问题描述：**
- `src/alice/services/memory_system.py` 实现了 3-tier 记忆系统（工作记忆、短期记忆、长期记忆）
- `MemoryContext` 对象已生成但**从未被 RankingService 使用**
- 用户当前关注的主题未影响推送内容

**修复方案：**

1. 修改 `RankingService.compute_p_score()` 接受 `MemoryContext` 参数
2. 根据 `working_memory` 提升相关概念内容的 `r_relevance` 权重
3. 新增 API 端点：`POST /api/v1/memory/working`，允许用户手动设置当前关注主题
4. 新增 Celery Beat 任务：每日执行记忆衰减

**涉及文件：**
- `src/alice/services/ranking.py`（修改）
- `src/alice/services/matching.py`（可能需要增加工作记忆加成）
- `src/alice/api/v1/`（新增 memory 路由）
- `src/alice/worker/scheduler.py`（新增衰减任务）

**验收标准：**
- 用户设置工作记忆后，相关内容优先推送
- 记忆随时间自动衰减
- Dashboard 显示当前记忆状态

---

### 2.3 FSRS 复习系统未接入用户流程

**问题描述：**
- `src/alice/services/fsrs_engine.py` 完整实现了 FSRS v5 算法
- 数据库已有 `review_cards` 表
- **但无 API 端点暴露复习功能，无定时任务生成复习卡片**
- 前端无复习界面

**修复方案：**

1. **后端：** 新增 API 路由 `src/alice/api/v1/review.py`
   - `GET /api/v1/review/due` → 获取当日到期的复习卡片
   - `POST /api/v1/review/submit` → 提交复习评分（1-5），更新 FSRS 参数
2. **任务：** 新增 Celery Beat 任务 `task_generate_review_cards`，每日生成新的复习卡片
3. **前端：** 新建页面 `frontend/src/app/review/page.tsx`，实现闪卡式复习界面

**涉及文件：**
- `src/alice/api/v1/review.py`（新建）
- `src/alice/worker/scheduler.py`（新增定时任务）
- `frontend/src/app/review/page.tsx`（新建）

**验收标准：**
- 用户可在前端看到到期的复习卡片
- 提交评分后，卡片的下次复习时间按 FSRS 算法更新
- Dashboard 显示复习统计（到期数、完成率）

---

### 2.4 语义搜索占位符实现

**问题描述：**
- `GraphRAGQueryEngine._semantic_search()` 方法当前仅返回空列表（占位符）
- `Content` 模型无 `embedding` 字段
- 无法实现基于语义的相似内容推荐

**修复方案：**

1. **数据库迁移：** 添加 `embedding` 字段（JSONB 或 VECTOR 类型，取决于 PostgreSQL 版本）
2. **Embedding 生成：** 在 `task_run_understanding` 阶段，调用 Embedding API（OpenAI/HuggingFace）生成向量
3. **语义搜索实现：** 使用 pgvector 或向量数据库（Qdrant/Weaviate）实现相似度查询
4. **GraphRAG 集成：** 完善 `_semantic_search()` 方法，返回实际结果

**涉及文件：**
- `alembic/versions/`（新增迁移文件）
- `src/alice/models/content.py`（添加 embedding 字段）
- `src/alice/services/understanding.py`（生成 embedding）
- `src/alice/services/graphrag_query.py`（实现语义搜索）

**验收标准：**
- 每条内容有对应的 embedding 向量
- GraphRAG 混合搜索包含语义相似度结果
- 前端搜索页面显示语义相似内容

---

### 2.5 p_score 批量更新定时任务缺失

**问题描述：**
- `p_score` 考虑时间衰减因素（T_timing），但当前仅在内容首次评分时计算
- 旧内容的 `p_score` 未随时间更新，可能不公平地占据推送优先级

**修复方案：**

新增 Celery Beat 任务：

```python
"update-p-scores-nightly": {
    "task": "alice.pipeline.tasks.task_update_p_scores",
    "schedule": crontab(hour=3, minute=0),  # 每天凌晨 3 点
    "options": {"queue": "pipeline"},
},
```

任务逻辑：
1. 查询所有 `pipeline_status=indexed` 且 `pushed_at IS NULL` 的内容
2. 批量重新计算 `p_score`
3. 更新数据库

**涉及文件：**
- `src/alice/pipeline/tasks.py`（新增任务）
- `src/alice/worker/scheduler.py`（添加到 Beat 调度）

**验收标准：**
- 旧内容的 `p_score` 随时间衰减
- 新鲜内容获得推送优先级

---

### 2.6 技能执行系统未触发

**问题描述：**
- `config/skills.yaml` 定义了技能（如 `weekly_cron` 触发的自我复盘技能）
- `src/alice/services/skill_executor.py` 已实现技能执行逻辑
- **但无 Celery 任务或 API 端点触发技能执行**

**修复方案：**

1. 新增 API 端点：`POST /api/v1/skills/execute/{skill_id}`，手动触发技能
2. 新增 Celery Beat 任务：根据 `skills.yaml` 中的 `weekly_cron` 配置，定时触发技能
3. 在 feedback 事件中，根据技能的 `trigger` 字段自动触发相关技能

**涉及文件：**
- `src/alice/api/v1/`（新增 skills 路由）
- `src/alice/worker/scheduler.py`（动态加载 skills.yaml，生成 Beat 任务）
- `src/alice/api/v1/feedback.py`（触发 feedback 相关技能）

**验收标准：**
- 每周自动生成自我复盘报告
- 用户反馈后，相关技能自动执行（如知识差距分析）
- Dashboard 显示技能执行历史

---

## 3. 🔵 开发体验与测试完善（P2）

### 3.1 前端 E2E 测试文件缺失

**问题描述：**
- `frontend/e2e/` 目录存在，Playwright 已配置
- **但该目录下没有任何测试文件**
- `package.json` 中有 `test:e2e` 脚本，但运行时无测试执行

**修复方案：**

创建核心流程的 E2E 测试：

1. `frontend/e2e/auth.spec.ts` - 登录流程
2. `frontend/e2e/feed.spec.ts` - 信息流浏览、反馈提交
3. `frontend/e2e/search.spec.ts` - 搜索功能
4. `frontend/e2e/settings.spec.ts` - 设置修改

**验收标准：**
- `npm run test:e2e` 成功运行至少 4 个测试文件
- CI 集成 E2E 测试

---

### 3.2 完整推送流水线集成测试缺失

**问题描述：**
- 当前集成测试覆盖各模块独立功能
- **无端到端测试覆盖：fetch → process → score → rank → push → feedback → KG update 完整流程**

**修复方案：**

创建 `tests/integration/test_full_pipeline.py`，包含：

1. 创建测试用户和测试源
2. 触发 `task_fetch_all_sources`
3. 等待所有 pipeline 任务完成
4. 验证内容已正确索引且 `p_score` 已计算
5. 调用 push API，验证推送批次排序正确
6. 模拟用户反馈，验证 KG 更新

**涉及文件：**
- `tests/integration/test_full_pipeline.py`（新建）

**验收标准：**
- 测试覆盖所有 pipeline 阶段
- 无 mock/patch（使用真实服务）
- 测试通过且稳定

---

### 3.3 Ollama 配置在 Linux 环境下无法工作

**问题描述：**
- `.env.example` 中 `OLLAMA_HOST=http://host.docker.internal:11434`
- 该地址仅在 macOS/Windows 的 Docker Desktop 中有效
- **Linux 环境下 `host.docker.internal` 不可用**

**修复方案：**

1. 在 `.env.example` 中注释说明：
   ```dotenv
   # For macOS/Windows Docker Desktop:
   OLLAMA_HOST=http://host.docker.internal:11434
   # For Linux:
   # OLLAMA_HOST=http://172.17.0.1:11434
   ```
2. 在 README 中添加 Linux 配置说明

**涉及文件：**
- `.env.example`（修改）
- `README.md`（补充文档）

**验收标准：**
- Linux 用户能根据文档正确配置 OLLAMA_HOST
- 测试环境中无硬编码的 `host.docker.internal`

---

### 3.4 观测性：无指标收集和监控

**问题描述：**
- 无 Prometheus 导出器
- 无请求追踪（request ID）
- 无 Celery 任务耗时统计
- 生产环境下无法诊断性能问题

**修复方案：**

1. **API 层：** 添加 `PrometheusMiddleware`，暴露 `/metrics` 端点
2. **Celery：** 集成 `celery-prometheus-exporter`
3. **日志：** 为每个请求生成 `request_id`，贯穿所有日志
4. **慢查询：** 记录 DB 查询耗时 > 1s 的日志

**涉及文件：**
- `src/alice/main.py`（添加中间件）
- `src/alice/worker/celery_app.py`（集成 Prometheus）
- `docker-compose.yml`（可选：添加 Prometheus/Grafana 服务）

**验收标准：**
- `/metrics` 端点返回 Prometheus 格式指标
- 日志包含 `request_id` 字段
- 可视化仪表板显示 API 和 Celery 性能

---

### 3.5 错误处理优化

**问题描述：**
- API 返回的错误信息不统一，无错误码
- 无死信队列（DLQ）处理永久失败的 Celery 任务
- 无告警机制

**修复方案：**

1. **统一错误响应格式：**
   ```json
   {
     "error_code": "CONTENT_NOT_FOUND",
     "message": "Content with ID 123 not found",
     "request_id": "abc-123"
   }
   ```
2. **Celery DLQ：** 配置 `task_reject_on_worker_lost=True`，失败任务进入专用队列
3. **告警：** 集成钉钉/飞书 webhook，关键错误自动通知

**涉及文件：**
- `src/alice/api/v1/`（统一异常处理）
- `src/alice/worker/celery_app.py`（配置 DLQ）

**验收标准：**
- 所有 API 错误返回统一格式
- 永久失败任务不丢失，可人工介入
- 关键错误自动告警

---

## 4. 🟣 技术债务清理（P3）

### 4.1 集成测试中仍有 mock/patch

**问题描述：**
- DESIGN.md 第 183 行明确指出："部分 integration 测试仍使用 patch 隔离网络或 Celery dispatch"
- 这违反了仓库的"强制质量原则 #3：生产路径不得引入模拟分支"

**修复方案：**

逐步替换 `patch()` 为真实服务调用：

1. 识别所有使用 `@patch` 的集成测试
2. 确保测试环境可访问真实服务（Docker Compose 或本地）
3. 重写测试，移除 mock

**涉及文件：**
- `tests/integration/` 下所有含 `@patch` 的测试文件

**验收标准：**
- 集成测试中无 `@patch` 装饰器（单元测试可保留）
- 测试仍然通过且稳定

---

### 4.2 Gatekeeper 规则回退逻辑

**问题描述：**
- `src/alice/services/gatekeeper.py` 在 Ollama 不可用时会回退到规则筛选
- 这违反了"生产路径不得引入模拟分支"原则

**修复方案：**

1. 移除规则回退逻辑
2. Ollama 不可用时，任务应快速失败并重试
3. 配置健康检查，确保 Ollama 服务始终可用

**涉及文件：**
- `src/alice/services/gatekeeper.py`（移除回退逻辑）
- `docker-compose.yml`（添加 Ollama 健康检查）

**验收标准：**
- Gatekeeper 仅使用 LLM 判断，无规则分支
- Ollama 不可用时任务失败并进入重试队列

---

### 4.3 Neo4j 初始化缺乏自动化

**问题描述：**
- Neo4j 图谱需要手动创建约束和索引
- 无自动初始化脚本

**修复方案：**

创建 `scripts/init_neo4j.py`，在应用启动时自动执行：

```cypher
CREATE CONSTRAINT user_id IF NOT EXISTS FOR (u:User) REQUIRE u.id IS UNIQUE;
CREATE CONSTRAINT concept_name IF NOT EXISTS FOR (c:Concept) REQUIRE c.name IS UNIQUE;
CREATE INDEX knows_mastery IF NOT EXISTS FOR ()-[r:KNOWS]-() ON (r.mastery);
```

**涉及文件：**
- `scripts/init_neo4j.py`（新建）
- `docker-compose.yml`（添加初始化 hook）

**验收标准：**
- 首次启动时自动创建 Neo4j 约束和索引
- 无需手动操作

---

### 4.4 技能执行结果未持久化

**问题描述：**
- 技能执行后的结果仅存在于日志中
- 无法追溯历史执行记录

**修复方案：**

1. 创建 `skill_execution_logs` 表：
   ```sql
   CREATE TABLE skill_execution_logs (
       id SERIAL PRIMARY KEY,
       user_id INTEGER REFERENCES users(id),
       skill_id VARCHAR(100),
       executed_at TIMESTAMP,
       result JSONB,
       success BOOLEAN
   );
   ```
2. 修改 `SkillExecutor`，执行后写入日志表

**涉及文件：**
- `alembic/versions/`（新增迁移）
- `src/alice/services/skill_executor.py`（添加持久化逻辑）

**验收标准：**
- Dashboard 显示技能执行历史
- API 端点可查询历史结果

---

## 5. 📊 数据模型补充建议

### 5.1 缺失的数据库字段

| 表名 | 缺失字段 | 用途 |
|------|---------|------|
| `content` | `embedding` | 语义搜索向量 |
| `users` | `last_pushed_date` | 推送频率控制 |
| `feedback` | - | （考虑增加聚合统计表 `feedback_stats`） |

### 5.2 缺失的数据表

| 表名 | 用途 | 优先级 |
|------|------|--------|
| `skill_execution_logs` | 技能执行历史 | P2 |
| `feedback_stats` | 用户反馈统计（避免重复计算） | P3 |
| `push_history` | 推送历史记录（当前依赖 `content.pushed_at`） | P3 |

---

## 6. 🎯 服务集成矩阵（当前状态 vs 目标状态）

| 服务组件 | 已实现 | 已测试 | API 暴露 | Pipeline 集成 | 定时任务 | 前端集成 |
|---------|-------|-------|---------|--------------|---------|---------|
| Gatekeeper | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| Understanding | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| Graph Extraction | ✅ | ✅ | Partial | ✅ | ❌ | ✅ |
| Scoring | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| Indexing | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| **MatchingService** | ✅ | ✅ | ❌ | ❌→✅ | ❌ | ❌ |
| **RankingService** | ✅ | ✅ | ❌ | ❌→✅ | ❌→✅ | ❌ |
| **FSRSEngine** | ✅ | ✅ | ❌→✅ | ❌ | ❌→✅ | ❌→✅ |
| **MemoryManager** | ✅ | ✅ | ❌→✅ | ❌→✅ | ❌→✅ | ❌→✅ |
| **PushScheduler** | ✅ | ✅ | ❌ | ❌→✅ | ❌→✅ | ✅ |
| **GraphRAG** | ✅ | ⚠️ | ❌→✅ | ❌ | ❌ | ❌→✅ |
| KGUpdater | ✅ | ✅ | ❌ | ⚠️→✅ | ❌ | ❌ |
| SkillExecutor | ✅ | ✅ | ❌→✅ | ❌ | ❌→✅ | ❌→✅ |
| CommunityDetection | ✅ | ✅ | ✅ | ❌ | ❌ | ✅ |
| Push (basic) | ✅ | ✅ | ✅ | ✅ | ⚠️→✅ | ✅ |
| Search (Meilisearch) | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ |
| Feedback (basic) | ✅ | ✅ | ✅ | ⚠️→✅ | ❌ | ✅ |

**图例：**
- ✅ = 已完成
- ⚠️ = 部分实现
- ❌ = 缺失
- ❌→✅ = 需要从缺失改为完成

---

## 7. 🗓️ 推荐实施路线图

### Sprint 1（1-2 周）：P0 紧急问题修复

- [ ] 集成 MatchingService 到推送批次
- [ ] 应用 RankingService 的 p_score 排序
- [ ] 添加 `task_retry_failed` 到 Beat 调度
- [ ] 暴露 GraphRAG 混合搜索 API
- [ ] 连接反馈 API 到 KGUpdater
- [ ] 修复 `.env.example` 缺失 ALICE_API_KEY

**交付标准：** 推送内容按实际相关性和质量排序，反馈闭环完整

---

### Sprint 2（2-3 周）：P1 功能完整性

- [ ] 实现 PushScheduler 定时任务（静默时段、推送窗口）
- [ ] 集成 MemoryManager 到排序逻辑
- [ ] 实现 FSRS 复习流程（API + 前端 + 定时任务）
- [ ] 添加 `embedding` 字段并实现语义搜索
- [ ] 实现 p_score 批量更新定时任务
- [ ] 触发技能执行系统（API + Beat + feedback 触发）

**交付标准：** 所有 Phase 2/3 特性可用，用户可见完整功能

---

### Sprint 3（1-2 周）：P2 开发体验提升

- [ ] 编写前端 E2E 测试（至少 4 个核心流程）
- [ ] 编写完整推送流水线集成测试
- [ ] 修复 Linux 环境下的 Ollama 配置问题
- [ ] 添加 Prometheus 指标导出
- [ ] 统一错误处理和错误码
- [ ] 实现死信队列和告警

**交付标准：** 测试覆盖率 > 80%，可观测性完善

---

### Sprint 4（1 周）：P3 技术债务清理

- [ ] 移除集成测试中的 mock/patch
- [ ] 移除 Gatekeeper 规则回退逻辑
- [ ] 自动化 Neo4j 初始化脚本
- [ ] 持久化技能执行日志
- [ ] 代码质量扫描和重构

**交付标准：** 代码符合"强制质量原则"，无技术债务

---

## 8. 📚 文档更新清单

随着功能实施，以下文档需要同步更新：

| 文档 | 需要更新的章节 |
|------|--------------|
| `README.md` | 环境变量说明、API 鉴权、新功能介绍 |
| `DESIGN.md` | 第 9 节 Roadmap（将"规划"改为"已实现"） |
| `AGENTS.md` | 新增技能执行、FSRS 复习等模块说明 |
| `.env.example` | 补充所有缺失环境变量 |
| `frontend/README.md` | 新页面说明（复习界面、记忆管理） |

---

## 9. 🚀 快速胜利（Quick Wins）

如果时间紧张，优先完成以下 3 项可获得最大收益：

1. **集成 MatchingService + RankingService 到推送排序（P0.1 + P0.2）**
   - 影响：推送内容质量立即提升
   - 工作量：2-3 天

2. **暴露 GraphRAG 混合搜索 API（P0.4）**
   - 影响：用户可体验知识图谱搜索
   - 工作量：1-2 天

3. **连接反馈 API 到 KGUpdater（P0.5）**
   - 影响：完成反馈闭环，系统开始"学习"
   - 工作量：1 天

---

## 10. 🧪 验收测试清单

完成所有修复后，运行以下测试确保系统正常：

### 后端测试
```bash
# 单元测试
uv run pytest tests/unit -v

# 集成测试（需要真实服务）
docker compose -f docker-compose.test.yml up -d
export TEST_DATABASE_URL="postgresql+asyncpg://alice:alice@localhost:5433/alice_test"
export NEO4J_TEST_URI="bolt://localhost:7687"
uv run pytest tests/integration -m integration -v

# 检查 Celery Beat 调度
docker compose logs scheduler | grep -E "(retry-failed|schedule-push|update-p-scores)"
```

### 前端测试
```bash
cd frontend
npm run test
npm run test:e2e
npm run lint
npm run typecheck
```

### 手动验收流程
1. 创建测试用户
2. 添加 RSS 源
3. 触发拉取任务
4. 等待 pipeline 处理完成
5. 查看推送批次（验证排序正确）
6. 提交反馈（验证 KG 更新）
7. 检查 Neo4j 中的 mastery 值
8. 访问前端复习页面（如已实现）

---

## 11. 📈 成功指标

完成本文档中的开发任务后，以下指标应得到改善：

| 指标 | 当前值 | 目标值 |
|------|-------|--------|
| 服务集成率 | ~60% | 100% |
| Celery Beat 任务数 | 1 | 6-8 |
| API 端点数量 | 38 | 45+ |
| 前端 E2E 测试数 | 0 | 10+ |
| 集成测试覆盖关键流程 | 部分 | 全部 |
| 用户反馈到 KG 更新延迟 | ∞（未实现） | < 5 秒 |
| 推送内容相关性（主观评分） | 3/5 | 4.5/5 |

---

## 12. ⚠️ 风险与注意事项

### 12.1 数据库迁移风险
- 添加 `embedding` 字段可能导致数据库膨胀（考虑单独的向量数据库）
- 迁移前务必备份生产数据

### 12.2 性能风险
- MatchingService 调用 Neo4j 可能较慢，考虑缓存或批处理
- 语义搜索的向量相似度计算耗时，需要优化（HNSW 索引）

### 12.3 兼容性风险
- 移除 Gatekeeper 规则回退后，Ollama 故障会导致内容堆积
- 需要监控告警确保 Ollama 高可用

---

## 13. 🤝 如何参与

### 认领任务
1. 在 GitHub Issues 中创建对应的 Issue（参考本文档章节号）
2. 标记优先级标签（P0/P1/P2/P3）
3. 评论"认领"并更新 Issue 状态

### 提交规范
- Commit 消息格式：`<type>(<scope>): <description>`
  - 示例：`feat(push): integrate MatchingService into push batch selection`
- 每个 PR 只解决一个问题（避免大型 PR）
- 必须包含测试（单元测试或集成测试）
- 更新相关文档（README/DESIGN/AGENTS）

### 代码审查清单
- [ ] 是否符合"强制开发原则"（无 TODO，真实依赖，无模拟分支）
- [ ] 是否有对应的测试
- [ ] 是否更新了文档
- [ ] 是否有数据库迁移（如有）
- [ ] 是否影响现有 API（需要版本管理）

---

## 14. 📞 获取帮助

如有疑问，请参考：

1. **技术问题：** 查看 `DESIGN.md` 和 `AGENTS.md`
2. **环境配置：** 查看 `README.md` 第 3-7 节
3. **架构决策：** 查看 `.sisyphus/plans/alice-ai-secretary.md`
4. **提交 Issue：** 使用本文档章节号作为 Issue 标题前缀

---

## 附录 A：文件路径速查表

| 功能模块 | 关键文件路径 |
|---------|------------|
| Pipeline 任务 | `src/alice/pipeline/tasks.py` |
| Celery Beat 调度 | `src/alice/worker/scheduler.py` |
| API 路由 | `src/alice/api/v1/*.py` |
| Service 层 | `src/alice/services/*.py` |
| LLM 抽象层 | `src/alice/llm/*.py` |
| 图谱操作 | `src/alice/graph/*.py` |
| 数据模型 | `src/alice/models/*.py` |
| 配置 | `src/alice/config/__init__.py` |
| 数据库迁移 | `alembic/versions/*.py` |
| 单元测试 | `tests/unit/*.py` |
| 集成测试 | `tests/integration/*.py` |
| 前端页面 | `frontend/src/app/*/page.tsx` |

---

**文档版本：** v1.0
**下次更新：** 完成 Sprint 1 后
**维护者：** 开发团队

---

## 🎯 总结

Alice 项目当前的主要问题不是**功能缺失**，而是**功能孤岛**。大量高质量的服务已经实现并通过测试，但它们尚未连接到用户可见的业务流程中。

**核心建议：**

1. **优先集成，而非开发新功能**
2. **聚焦 P0 问题，快速见效**
3. **补全测试，确保稳定性**
4. **持续更新文档，保持一致性**

完成本文档中的开发任务后，Alice 将从"功能原型"升级为"生产就绪"的个人智能信息管理系统。

---

**最后更新：** 2026-02-27
**分析工具：** Claude Code Explore Agent + Manual Code Review
**总耗时：** 约 2 小时深度分析
